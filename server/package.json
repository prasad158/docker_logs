{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node_modules/.bin/tsc -p tsconfig.json",
    "start": "rm -rf ./dist && npm run build && node ./dist/server.js"
  },
  "author": "Prasad Bamane",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.4.3",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10"
  }
}